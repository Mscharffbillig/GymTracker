# Analytics and feedback endpoint URLs.
# EXPO_PUBLIC_* variables are inlined by Metro at build time (no runtime file read needed).
# Leave blank to disable the feature gracefully (analytics no-ops; feedback shows mock in dev).

EXPO_PUBLIC_ANALYTICS_URL=
EXPO_PUBLIC_FEEDBACK_URL=
